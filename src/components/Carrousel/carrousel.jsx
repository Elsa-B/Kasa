//import Data from "../../data/data.json"

function Carrousel(){
    return
        /*<div>
		{Data.map((slide) => (
			<div key={slide.id} >
				<img src={slide.pictures} alt="logement" />
			</div>
		))}
        </div>*/
    
}

export default Carrousel;